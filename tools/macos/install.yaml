name: MacOS
description: Configure MacOS
stage: 0
config: []

scripts:
  macos_arm: |
    # Sets default to show all hidden files
    # defaults write com.apple.finder AppleShowAllFiles -boolean true; killall Finder;